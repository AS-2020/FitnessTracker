<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:FitnessTracker.ViewModels"
             x:Class="FitnessTracker.Pages.Menu">
    <ContentPage.BindingContext>
        <vm:MainVm/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Button Text="New Entry"  Command="{Binding NavigationCommand}" CommandParameter="NewEntry"/>
            <!--Clicked="NewEntry_Clicked"-->
            <Button Text="View Entries" Command="{Binding NavigationCommand}" CommandParameter="ViewEntries"/>
            <Button Text="Delete Entries" Command="{Binding NavigationCommand}" CommandParameter="DeleteEntries"/>
            <Button Text="Exit" Command="{Binding ExitCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>