<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:vm="clr-namespace:FitnessTracker.ViewModels"
             x:Class="FitnessTracker.Pages.DeleteEntries">
    <ContentPage.BindingContext>
        <vm:MainVm/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Entry Placeholder="Password" Text="{Binding Password}"/>
            <Button Text="Delete BodyWeight" Command="{Binding DeleteBodyWeightCommand}"/>
            <Button Text="Delete Jogging" Command="{Binding DeleteJoggingCommand}"/>
            <Button Text="Back" Command="{Binding NavigationCommand}" CommandParameter="Back"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>