<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:Microcharts.Forms;assembly=Microcharts.Forms"
             xmlns:vm="clr-namespace:FitnessTracker.ViewModels"
             x:Class="FitnessTracker.Pages.ViewBodyWeightChart">
    <ContentPage.BindingContext>
        <vm:MainVm/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid >
            <Grid.RowDefinitions >
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="8*"/>
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" Orientation="Horizontal">
                <Entry Text="{Binding CharDateBodyWeightMin}" Placeholder="Start Date" WidthRequest="100" HeightRequest="50"/>
                <Entry Text="{Binding CharDateBodyWeightMax}" Placeholder="End Date" WidthRequest="100" HeightRequest="50"/>
                <Button Text="print Chart" WidthRequest="100" HeightRequest="50" Grid.Row="0" Command="{Binding CreateBodyWeightChartCommand}" />
            </StackLayout>
            <Label Grid.Row="1" Text="{Binding ErrorDateBodyWeightChartView}" HorizontalOptions="Center" FontSize="16"/>
            <forms:ChartView Grid.Row="2" Chart="{Binding ChartBodyWeight}" HeightRequest="150"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>